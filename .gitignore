# Go workspace
go.work.sum

# Environment
.env
.env.local

# Node & Next.js
node_modules/
.next/
out/

# Database & State (CRITICAL)
*.db
*.db-shm
*.db-wal
deploy/headscale/data/*
!deploy/headscale/data/.gitkeep

# Dynamic DNS edits — ignore substituted runtime zone files but keep the template
deploy/coredns/zones/_acme-challenge.*
!deploy/coredns/zones/_acme-challenge.{DOMAIN}
